[build]
  # Generate js/config.js (gitignored) from environment variables before publish.
  # Set WORKER_URL in Netlify → Site settings → Env vars.
  publish = "."
  command = "sh scripts/gen-config.sh"
